### 3.8 带外数据
```
TCP利用头部中的紧急指针标志和紧急指针两个字段，给应用程序提供了一种紧急方式

发送端一次发送的多字节带外数据中(abc)只有最后一字节被当作带外数据(c)，
而其他数据(ab)被当成了普通数据。

如果TCP模块以多个TCP报文段来发送缓冲区里面的内容，则每个TCP报文段都将设置URG标志
它们的紧急指针都指向同一个位置（带外数据的下一个字节）

但只有一个TCP报文段真正携带带外数据

TCP接收端会根据紧急指针所指的位置确定带外数据的位置，
并将它读入1字节的带外缓存中（可以被后续的带外数据覆盖）。。
```
